<h1>choose a movie to add to <%= @list.name %>!</h1>
<legend>

<div class="form-check" :first-of-type>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.button :submit %>
  <% end %>
</div>
